import type { Session } from '../instance/index.js';
export declare function addReaction(this: Session, { messageTimestamp, messageAuthor, emoji }: {
    messageTimestamp: number;
    emoji: string;
    messageAuthor: string;
}): Promise<void>;
export declare function removeReaction(this: Session, { messageTimestamp, messageAuthor, emoji }: {
    messageTimestamp: number;
    emoji: string;
    messageAuthor: string;
}): Promise<void>;
