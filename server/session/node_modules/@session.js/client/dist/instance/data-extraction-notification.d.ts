import type { Session } from '../instance/index.js';
export declare function notifyScreenshotTaken(this: Session, { conversation }: {
    conversation: string;
}): Promise<void>;
export declare function notifyMediaSaved(this: Session, { conversation, savedMessageTimestamp }: {
    conversation: string;
    savedMessageTimestamp: number;
}): Promise<void>;
