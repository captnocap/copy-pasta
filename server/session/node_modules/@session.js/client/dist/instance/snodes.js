import { RequestType } from '@session.js/types/network/request';
export async function getSnodes() {
    if (!this.snodes) {
        const { snodes } = await this._request({ type: RequestType.GetSnodes, body: {} });
        this.snodes = snodes;
    }
    return this.snodes;
}
