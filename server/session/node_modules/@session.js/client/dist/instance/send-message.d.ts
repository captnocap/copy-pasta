import type { Session } from '../instance/index.js';
export declare function sendMessage(this: Session, { to, text, attachments, voiceMessage, replyToMessage }: {
    to: string;
    text?: string;
    attachments?: File[];
    voiceMessage?: Blob;
    replyToMessage?: {
        /** Timestamp of the quoted message received in sentMessage */
        timestamp: number;
        /** Session ID of person who sent the quoted message */
        author: string;
        /** Text of message or part of it to quote */
        text?: string;
        /** Attachments of quoted message to show in preview */
        attachments?: {
            contentType?: string;
            fileName?: string;
        }[];
    };
}): Promise<{
    messageHash: string;
    syncMessageHash: string;
    timestamp: number;
}>;
