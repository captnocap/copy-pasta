import type { Session } from '../instance/index.js';
export declare function deleteMessage(this: Session, { to, timestamp, hash }: {
    to: string;
    timestamp: number;
    hash: string;
}): Promise<void>;
export declare function deleteMessages(this: Session, messages: {
    to: string;
    timestamp: number;
    hash: string;
}[]): Promise<void>;
