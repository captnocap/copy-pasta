import type { Session } from '../instance/index.js';
export declare function showTypingIndicator(this: Session, { conversation }: {
    conversation: string;
}): Promise<void>;
export declare function hideTypingIndicator(this: Session, { conversation }: {
    conversation: string;
}): Promise<void>;
