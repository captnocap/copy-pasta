export declare function decryptAttachment(data: ArrayBuffer, { size, keyBuffer, digestBuffer }: {
    size?: number;
    keyBuffer: Uint8Array;
    digestBuffer: Uint8Array;
}): Promise<ArrayBuffer>;
