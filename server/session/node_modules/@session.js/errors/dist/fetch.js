import { SessionJsError } from "./session-js.js";
export var SessionFetchErrorCode;
(function (SessionFetchErrorCode) {
    SessionFetchErrorCode["RetryWithOtherNode421Error"] = "retry_with_other_node_421_error";
    SessionFetchErrorCode["NoSnodesAvailable"] = "no_snodes_available";
    SessionFetchErrorCode["NoSwarmsAvailable"] = "no_swarms_available";
    SessionFetchErrorCode["FetchFailed"] = "fetch_failed";
    SessionFetchErrorCode["InvalidResponse"] = "invalid_response";
    SessionFetchErrorCode["PollingFailed"] = "polling_failed";
    SessionFetchErrorCode["UploadFailed"] = "upload_failed";
    SessionFetchErrorCode["UnknownMethod"] = "unknown_method";
    SessionFetchErrorCode["Generic"] = "generic";
})(SessionFetchErrorCode || (SessionFetchErrorCode = {}));
/** Generic error for cases where developer does something incorrectly */
export class SessionFetchError extends SessionJsError {
    code;
    constructor({ code, message }) {
        super({ code, message });
        this.code = code;
    }
}
