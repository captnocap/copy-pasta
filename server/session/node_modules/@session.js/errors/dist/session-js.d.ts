/** Most generic error class that other errors extend from */
export declare class SessionJsError extends Error {
    code: string;
    constructor({ code, message }: {
        code: string;
        message: string;
    });
}
