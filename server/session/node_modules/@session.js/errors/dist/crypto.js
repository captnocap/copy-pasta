import { SessionJsError } from "./session-js.js";
export var SessionCryptoErrorCode;
(function (SessionCryptoErrorCode) {
    SessionCryptoErrorCode["MessageEncryptionFailed"] = "message_encryption_failed";
    SessionCryptoErrorCode["MessageDecryptionFailed"] = "message_decryption_failed";
    SessionCryptoErrorCode["MessageVerificationFailed"] = "message_verification_failed";
    SessionCryptoErrorCode["AttachmentDecryptionFailed"] = "attachment_decryption_failed";
    SessionCryptoErrorCode["AttachmentEncryptionFailed"] = "attachment_encryption_failed";
})(SessionCryptoErrorCode || (SessionCryptoErrorCode = {}));
/** Generic error for cases where developer does something incorrectly */
export class SessionCryptoError extends SessionJsError {
    code;
    constructor({ code, message }) {
        super({ code, message });
        this.code = code;
    }
}
